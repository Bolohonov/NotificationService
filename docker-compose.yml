version: '3.9'
services:
  eureka-server:
    image: eureka-server
    build: eureka-server
    container_name: eureka_server_container
    ports:
      - "8761:8761"

  api-gateway:
    image: api-gateway
    build: api-gateway
    container_name: api_gateway_container
    ports:
      - "9090:9090"
    environment:
      jwt_secret: ecf1222c-88fd-47f3-988f-bdd78bade1ad
        - eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka/
        - NS_SERVER_URI=http://ns-server:8085
    depends_on:
      - eureka-server
      - ns-server
    links:
      - eureka-server
    restart: always

  ns-server:
    image: ns-server
    build: ns-server
    container_name: server_container
    ports:
      - "8085:8085"
    environment:
      jwt_secret: ecf1222c-88fd-47f3-988f-bdd78bade1ad
        - eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka/
    depends_on:
      - eureka-server
    links:
      - eureka-server

  auth-server:
    image: auth-server
    container_name: auth-server_container
    hostname: auth-server
    expose:
      - "8080"
    environment:
      jwt_secret: ecf1222c-88fd-47f3-988f-bdd78bade1ad
        - eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka/
    restart: always
    depends_on:
      - eureka-server

  redis:
    image: 'bitnami/redis:latest'
    hostname: redis
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    expose:
      - "6379"
