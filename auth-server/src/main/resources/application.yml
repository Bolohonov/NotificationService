server:
  port: 8090
  error:
    include-stacktrace: never

spring:
  application:
    name: auth-server

jwt:
  secret: ${JWT_SECRET}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
